{
  "name": "connectors-api-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The host's operator page, and the harness that executes it.",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1"
  }
}
