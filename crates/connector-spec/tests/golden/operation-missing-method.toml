# An operation with no method. There is no default: guessing GET would turn a forgotten `method`
# into a silently different request.
id = "zendesk"
base_url = "https://acme.zendesk.com"

[[operations]]
id = "zendesk.ticket.show"
path = "/api/v2/tickets/{ticket_id}.json"
risk = "low"
idempotency = "idempotent"
auth = []
