// Generated by flux-connectors 0.8.0 — do not edit.
// Provider: notion
// Regenerate with `flux-connectors build`.
//
// The `include_str!` paths point at `crates/catalog/ops/notion/`, which the same build wrote.

#[rustfmt::skip]
pub(crate) static PROVIDER: crate::Provider = crate::Provider {
    id: "notion",
    vendor: "Notion",
    description: "Notion workspace: read a page's properties, query a database, search shared content, and create a page",
    authority: Some("com.notion.api"),
    runtime: crate::Runtime::Http,
    base_url: "https://api.notion.com",
    auth: AUTH,
    operations: OPERATIONS,
};

#[rustfmt::skip]
static AUTH: &[crate::Credential] = &[
    crate::Credential {
        name: "notion.token",
        leaf: "token",
        acquire: crate::Acquisition::Static,
        place: crate::Placement::Header { name: "Authorization", prefix: "Bearer " },
    },
];

#[rustfmt::skip]
static OPERATIONS: &[crate::Operation] = &[
    crate::Operation {
        id: "notion-user-me",
        provider: "notion",
        service: "default",
        description: "Get the bot user this integration authenticates as, confirming the token resolves and naming the workspace it belongs to. Takes no parameters",
        risk: crate::Risk::Low,
        idempotency: crate::Idempotency::Idempotent,
        credentials: &[&["notion.token"]],
        hosts: &["api.notion.com"],
        flux: include_str!("../../ops/notion/notion-user-me.flux"),
    },
    crate::Operation {
        id: "notion-page-get",
        provider: "notion",
        service: "default",
        description: "Get one page's properties, parent, icon, cover and timestamps. This does NOT return the page's text — in Notion a page's content is a separate tree of blocks, which this connector cannot read",
        risk: crate::Risk::Low,
        idempotency: crate::Idempotency::Idempotent,
        credentials: &[&["notion.token"]],
        hosts: &["api.notion.com"],
        flux: include_str!("../../ops/notion/notion-page-get.flux"),
    },
    crate::Operation {
        id: "notion-database-query",
        provider: "notion",
        service: "default",
        description: "Query a database and return its first page of entries (up to 100), unfiltered and in the database's default order. Each entry is a page, and its `properties` are keyed by that database's own column names. Notion routes this read through POST, so it is declared as a write",
        risk: crate::Risk::Medium,
        idempotency: crate::Idempotency::NonIdempotent,
        credentials: &[&["notion.token"]],
        hosts: &["api.notion.com"],
        flux: include_str!("../../ops/notion/notion-database-query.flux"),
    },
    crate::Operation {
        id: "notion-search",
        provider: "notion",
        service: "default",
        description: "Search the pages and databases shared with this integration by title, returning the first page of matches (up to 100). Notion matches on title only — it does not search page text. Notion routes this read through POST, so it is declared as a write",
        risk: crate::Risk::Medium,
        idempotency: crate::Idempotency::NonIdempotent,
        credentials: &[&["notion.token"]],
        hosts: &["api.notion.com"],
        flux: include_str!("../../ops/notion/notion-search.flux"),
    },
    crate::Operation {
        id: "notion-page-create",
        provider: "notion",
        service: "default",
        description: "Create a new empty page as a child of an existing page. The page is created with a title and no content — this connector cannot write page body text, which in Notion is a separate tree of blocks. The parent page must be shared with this integration",
        risk: crate::Risk::Medium,
        idempotency: crate::Idempotency::NonIdempotent,
        credentials: &[&["notion.token"]],
        hosts: &["api.notion.com"],
        flux: include_str!("../../ops/notion/notion-page-create.flux"),
    },
];
