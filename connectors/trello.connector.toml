# Generated by flux-connectors 0.7.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.7.0"
connector = "trello"
gid = "com.trello.api:1"
vendor = "Trello"
description = "Trello boards and cards: list the boards a token can see, read a board and its lists, read a list's cards, create a card, and archive one. Authenticates every request with an API key and a token together, both in the query string"
base_url = "https://api.trello.com/1"
api_version = "1"
module = "trello.flux"
operations = ["trello-board-list", "trello-board-get", "trello-board-lists", "trello-list-cards", "trello-card-create", "trello-card-archive"]
