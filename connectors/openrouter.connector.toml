# Generated by flux-connectors 0.26.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.26.0"
connector = "openrouter"
gid = "ai.openrouter.api:v1"
vendor = "OpenRouter"
description = "OpenRouter model router: list models and their endpoints, create chat completions, and read credit balance"
runtime = "http"
base_url = "https://openrouter.ai"
api_version = "v1"
module = "openrouter.flux"
operations = ["openrouter-models-list", "openrouter-model-endpoints-list", "openrouter-chat-completion", "openrouter-credits-get"]

[operation_directions]
openrouter-chat-completion = "write"
openrouter-credits-get = "read"
openrouter-model-endpoints-list = "read"
openrouter-models-list = "read"

[operation_semantic_effects]
openrouter-chat-completion = []
openrouter-credits-get = []
openrouter-model-endpoints-list = []
openrouter-models-list = []

[[auth]]
name = "openrouter.api_key"
scheme = "bearer"
env = ["OPENROUTER_API_KEY"]
description = "OpenRouter API key, sent as a bearer token"

[[default_auth]]
credentials = ["openrouter.api_key"]
