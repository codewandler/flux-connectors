# Generated by flux-connectors 0.19.1 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.19.1"
connector = "calendly"
vendor = "Calendly"
description = "Calendly scheduling: read the authenticated user, event types, scheduled events and their invitees"
runtime = "http"
base_url = "https://api.calendly.com"
module = "calendly.flux"
operations = ["calendly-user-me", "calendly-event-type-list", "calendly-scheduled-event-list", "calendly-scheduled-event-get", "calendly-invitee-list"]
verify = "calendly-user-me"

[operation_semantic_effects]
calendly-event-type-list = []
calendly-invitee-list = []
calendly-scheduled-event-get = []
calendly-scheduled-event-list = []
calendly-user-me = []

[[auth]]
name = "calendly.access_token"
scheme = "bearer"
env = ["CALENDLY_ACCESS_TOKEN"]
description = "Calendly access token sent as a bearer — a personal access token or an OAuth-issued one"

[[default_auth]]
credentials = ["calendly.access_token"]

[[config]]
name = "access_token"
label = "Calendly access token"
help = "Create one under Integrations & apps → API & Webhooks → Personal access tokens in Calendly. Calendly shows it once"
format = "token"
secret = true
docs_url = "https://developer.calendly.com/getting-started"
binds = "credential.calendly.access_token"
level = "connection"
