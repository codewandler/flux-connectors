# Generated by flux-connectors 0.22.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.22.0"
connector = "babelforce"
service = "task-automation"
gid = "com.babelforce.api/task-automation:v3"
vendor = "Babelforce"
description = "Babelforce voice platform: agent availability, live and reported calls, and the session variables a flow reads and writes. The full manager-sdk surface — agents, calls, sessions, outbound, phonebook, reporting, task automation and scheduling — is catalogued and callable; the operations above are the ones a model drives directly"
runtime = "http"
base_url = "https://services.babelforce.com"
api_version = "v3"
module = "babelforce-task-automation.flux"
operations = ["babelforce-change-agent-lock", "babelforce-agent-action-on-task", "babelforce-tasks", "babelforce-submit-task", "babelforce-testing", "babelforce-list-secret-prefixes", "babelforce-list-secret-keys", "babelforce-create-secrets", "babelforce-patch-secrets", "babelforce-delete-secret-keys", "babelforce-read-selection-configuration", "babelforce-create-selection-configuration", "babelforce-update-selection-configuration", "babelforce-delete-selection-configuration", "babelforce-list", "babelforce-agent-interaction-duration", "babelforce-agent-interactions", "babelforce-task-journal", "babelforce-list-scripts", "babelforce-submit-script", "babelforce-get-script", "babelforce-update-script", "babelforce-delete-script", "babelforce-submit-task-template", "babelforce-task-usage", "babelforce-task-usage-types", "babelforce-task", "babelforce-update-task", "babelforce-manager-interrupt-on-task", "babelforce-change-task-state"]

[operation_directions]
babelforce-agent-action-on-task = "write"
babelforce-agent-interaction-duration = "read"
babelforce-agent-interactions = "read"
babelforce-change-agent-lock = "write"
babelforce-change-task-state = "write"
babelforce-create-secrets = "write"
babelforce-create-selection-configuration = "write"
babelforce-delete-script = "write"
babelforce-delete-secret-keys = "write"
babelforce-delete-selection-configuration = "write"
babelforce-get-script = "read"
babelforce-list = "read"
babelforce-list-scripts = "read"
babelforce-list-secret-keys = "read"
babelforce-list-secret-prefixes = "read"
babelforce-manager-interrupt-on-task = "write"
babelforce-patch-secrets = "write"
babelforce-read-selection-configuration = "read"
babelforce-submit-script = "write"
babelforce-submit-task = "write"
babelforce-submit-task-template = "write"
babelforce-task = "read"
babelforce-task-journal = "read"
babelforce-task-usage = "read"
babelforce-task-usage-types = "read"
babelforce-tasks = "read"
babelforce-testing = "write"
babelforce-update-script = "write"
babelforce-update-selection-configuration = "write"
babelforce-update-task = "write"

[operation_semantic_effects]
babelforce-agent-action-on-task = []
babelforce-agent-interaction-duration = []
babelforce-agent-interactions = []
babelforce-change-agent-lock = []
babelforce-change-task-state = []
babelforce-create-secrets = []
babelforce-create-selection-configuration = []
babelforce-delete-script = []
babelforce-delete-secret-keys = []
babelforce-delete-selection-configuration = []
babelforce-get-script = []
babelforce-list = []
babelforce-list-scripts = []
babelforce-list-secret-keys = []
babelforce-list-secret-prefixes = []
babelforce-manager-interrupt-on-task = []
babelforce-patch-secrets = []
babelforce-read-selection-configuration = []
babelforce-submit-script = []
babelforce-submit-task = []
babelforce-submit-task-template = []
babelforce-task = []
babelforce-task-journal = []
babelforce-task-usage = []
babelforce-task-usage-types = []
babelforce-tasks = []
babelforce-testing = []
babelforce-update-script = []
babelforce-update-selection-configuration = []
babelforce-update-task = []

[[auth]]
name = "babelforce.access_token"
scheme = "bearer"
env = ["BABELFORCE_ACCESS_TOKEN"]
description = "babelforce access token. Either supplied through the environment, or obtained by the host running one of the grants below"
subject = "user"
hazard = "resource_owner_secret_shared"

[auth.oauth2]
token_path = "/oauth/token"
scopes = ["*"]
grants = ["password", "refresh_token"]

[[auth.quirks.token_endpoint]]
grant = "client_credentials"
behaviour = "`expires_in` is read from the request and defaults to `-1`, which the service reads as *never expires*. An unattended caller that omits it therefore gets a non-expiring token rather than a short-lived one."
attribution = "babelforce's own `AuthController.token()`, read against `specs/babelforce/auth-2026-06-25.openapi.yaml`, which declares no request-side `expires_in`. Recorded from the owner's measurement in flux-exchange's credential-acquisition epic (C-440)."
measured = "2026-08-02"

[[auth.quirks.token_endpoint]]
grant = "password"
behaviour = "`expires_in` is read from the request when present, and the service default applies when it is absent."
attribution = "babelforce's own `AuthController.token()`, read against `specs/babelforce/auth-2026-06-25.openapi.yaml`, which declares no request-side `expires_in`. Recorded from the owner's measurement in flux-exchange's credential-acquisition epic (C-440)."
measured = "2026-08-02"

[[auth.quirks.token_endpoint]]
grant = "refresh_token"
behaviour = "`expires_in` is read from the request and passed into the refresh. Separately, `account_id` on the request **switches the account** the new token belongs to — nothing in RFC 6749 makes a refresh change whose token it is, so a host that rotates a credential without pinning `account_id` can silently move it to another account."
attribution = "babelforce's own `AuthController.token()`, read against `specs/babelforce/auth-2026-06-25.openapi.yaml`, which declares neither a request-side `expires_in` nor `account_id`. Recorded from the owner's measurement in flux-exchange's credential-acquisition epic (C-440)."
measured = "2026-08-02"

[[auth.quirks.token_endpoint]]
grant = "link"
behaviour = "A fifth `grant_type` that no vendored document declares at all. `expires_in` is read from the request and then clamped to **at most 60 seconds**, so a lifetime asked for here is a ceiling rather than a request."
attribution = "babelforce's own `AuthController.token()`, read against `specs/babelforce/auth-2026-06-25.openapi.yaml:211-220`, whose `grant_type` enum names four grants and not this one. Recorded from the owner's measurement in flux-exchange's credential-acquisition epic (C-440)."
measured = "2026-08-02"

[[auth.quirks.token_endpoint]]
grant = "authorization_code"
behaviour = "`expires_in` is **not read**. Only `access_type` is, defaulting to `offline`. This row is a measurement and not a gap: the one grant that ignores the field is the fact a reader most needs, because it is the one where asking for a lifetime silently does nothing."
attribution = "babelforce's own `AuthController.token()`, read against `specs/babelforce/auth-2026-06-25.openapi.yaml`, which declares no request-side `expires_in`. Recorded from the owner's measurement in flux-exchange's credential-acquisition epic (C-440)."
measured = "2026-08-02"

[[default_auth]]
credentials = ["babelforce.access_token"]
