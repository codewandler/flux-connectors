# Generated by flux-connectors 0.1.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.1.0"
connector = "slack"
vendor = "Slack"
description = "Slack messaging: post messages, read conversation history, look up users, react"
base_url = "https://slack.com"
module = "slack.flux"
operations = ["slack-chat-post-message", "slack-conversations-history", "slack-users-info", "slack-reactions-add"]
