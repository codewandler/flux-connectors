# Generated by flux-connectors 0.22.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.22.0"
connector = "dropbox"
gid = "com.dropbox.api:2"
vendor = "Dropbox"
description = "Dropbox: browse folder contents, read file and folder metadata, search files, create folders, and get temporary download links"
runtime = "http"
base_url = "https://api.dropboxapi.com"
api_version = "2"
module = "dropbox.flux"
operations = ["dropbox-user-me", "dropbox-folder-list", "dropbox-metadata-get", "dropbox-search", "dropbox-folder-create", "dropbox-temporary-link-get"]
verify = "dropbox-user-me"

[operation_directions]
dropbox-folder-create = "write"
dropbox-folder-list = "write"
dropbox-metadata-get = "write"
dropbox-search = "write"
dropbox-temporary-link-get = "write"
dropbox-user-me = "read"

[operation_semantic_effects]
dropbox-folder-create = []
dropbox-folder-list = []
dropbox-metadata-get = []
dropbox-search = []
dropbox-temporary-link-get = []
dropbox-user-me = []

[[auth]]
name = "dropbox.access_token"
scheme = "bearer"
env = ["DROPBOX_ACCESS_TOKEN"]
description = "Dropbox access token sent as a bearer — a scoped app access token or an OAuth2 access token"

[[default_auth]]
credentials = ["dropbox.access_token"]

[[config]]
name = "access_token"
label = "Access token"
help = "Create an app at dropbox.com/developers/apps. For a quick connection, generate an access token directly from the app's Settings tab. For a token that keeps working for other users, complete the app's OAuth2 authorization flow and supply the resulting access token"
format = "token"
secret = true
docs_url = "https://developers.dropbox.com/oauth-guide"
binds = "credential.dropbox.access_token"
level = "connection"
