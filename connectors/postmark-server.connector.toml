# Generated by flux-connectors 0.7.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.7.0"
connector = "postmark"
service = "server"
vendor = "Postmark"
description = "Postmark transactional email: send a message, read delivery stats and bounces. Account: list and inspect servers, authenticated separately from sending"
base_url = "https://api.postmarkapp.com"
module = "postmark-server.flux"
operations = ["postmark-email-send", "postmark-deliverystats-get", "postmark-bounce-list", "postmark-bounce-get"]
