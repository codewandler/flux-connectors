# Generated by flux-connectors 0.25.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.25.0"
connector = "twilio"
gid = "com.twilio.api:2010-04-01"
vendor = "Twilio"
description = "Twilio programmable messaging and voice: read the account, messages, calls, recordings, usage and conferences"
runtime = "http"
base_url = "https://api.twilio.com/2010-04-01"
api_version = "2010-04-01"
module = "twilio.flux"
operations = ["twilio-account-get", "twilio-message-list", "twilio-message-get", "twilio-call-list", "twilio-call-get", "twilio-recording-list", "twilio-recording-get", "twilio-usage-record-list", "twilio-conference-list"]
verify = "twilio-account-get"

[operation_directions]
twilio-account-get = "read"
twilio-call-get = "read"
twilio-call-list = "read"
twilio-conference-list = "read"
twilio-message-get = "read"
twilio-message-list = "read"
twilio-recording-get = "read"
twilio-recording-list = "read"
twilio-usage-record-list = "read"

[operation_semantic_effects]
twilio-account-get = []
twilio-call-get = []
twilio-call-list = []
twilio-conference-list = []
twilio-message-get = []
twilio-message-list = []
twilio-recording-get = []
twilio-recording-list = []
twilio-usage-record-list = []

[[auth]]
name = "twilio.basic_auth"
scheme = "basic"
env = ["TWILIO_AUTH_TOKEN"]
user_env = ["TWILIO_ACCOUNT_SID"]
description = "Twilio Account SID and Auth Token, sent as HTTP Basic credentials"

[[auth]]
name = "twilio.webhook_signing_secret"
scheme = "signing"
env = ["TWILIO_AUTH_TOKEN"]
description = "The same Auth Token as twilio.basic_auth, used here to verify X-Twilio-Signature on inbound status callbacks. Never sent to Twilio"

[[default_auth]]
credentials = ["twilio.basic_auth"]

[[config]]
name = "account_sid"
label = "Account SID"
help = "Copy this from the Twilio Console dashboard — it starts with AC and is the same value used to sign in to the API, not your login email"
example = "ACXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
format = "token"
docs_url = "https://www.twilio.com/docs/iam/api/account"
binds = "username.twilio.basic_auth"
also_binds = ["path.AccountSid"]
level = "connection"

[[config]]
name = "auth_token"
label = "Auth Token"
help = "Your primary Auth Token from the Twilio Console dashboard. Twilio shows it masked by default — click to reveal it"
format = "token"
secret = true
docs_url = "https://www.twilio.com/docs/iam/api/account"
binds = "credential.twilio.basic_auth"
level = "connection"

[[events]]
name = "message.status_callback"
oip = "com.twilio.api:2010-04-01#message.status_callback"
description = "A message's delivery status changed. Twilio POSTs this, form-encoded, to the StatusCallback URL supplied when the message was created"
group = "Messaging"

[[events]]
name = "call.status_callback"
oip = "com.twilio.api:2010-04-01#call.status_callback"
description = "A call's status changed. Twilio POSTs this, form-encoded, to the StatusCallback URL configured for the call or the answering number"
group = "Voice"

[[channels]]
name = "message-status-callback"
oip = "com.twilio.api:2010-04-01#message-status-callback"
description = "Message delivery-status callbacks. Twilio POSTs form-encoded to the StatusCallback URL supplied when a message is created, signing the URL and the parameters with the Auth Token"
transport = "webhook"
events = ["message.status_callback"]

[channels.verification]
kind = "hmac"
verified = true

[channels.verification.hmac]
algorithm = "sha1"
encoding = "base64"
header = "X-Twilio-Signature"
signed = "{url}{sorted_form}"
secret = "twilio.webhook_signing_secret"

[channels.payload]
error_code = "ErrorCode"
message_sid = "MessageSid"
sender = "From"
status = "MessageStatus"
to = "To"

[channels.setup]
docs_url = "https://www.twilio.com/docs/messaging/guides/track-outbound-message-status"
steps = ["Open console.twilio.com and choose the phone number or Messaging Service this connection sends from", "Paste the Request URL shown above into its status-callback field, exactly as shown — the signature covers the URL, so a redirect or a rewritten host breaks every delivery", "Or, if messages are created by another system, have it pass the same URL as the `StatusCallback` parameter", "Copy the Auth Token from the console into this connection's signing secret field — it is the same value as the Auth Token above"]

[[channels]]
name = "call-status-callback"
oip = "com.twilio.api:2010-04-01#call-status-callback"
description = "Call status callbacks. Twilio POSTs form-encoded to the StatusCallback URL configured for the call or the answering number, signed exactly as the messaging callback is"
transport = "webhook"
events = ["call.status_callback"]

[channels.verification]
kind = "hmac"
verified = true

[channels.verification.hmac]
algorithm = "sha1"
encoding = "base64"
header = "X-Twilio-Signature"
signed = "{url}{sorted_form}"
secret = "twilio.webhook_signing_secret"

[channels.payload]
call_sid = "CallSid"
caller = "From"
direction = "Direction"
duration = "CallDuration"
status = "CallStatus"
to = "To"

[channels.setup]
docs_url = "https://www.twilio.com/docs/voice/twiml/number#attributes-statusCallback"
steps = ["Open console.twilio.com and choose the phone number this connection answers or dials on", "Paste the Request URL shown above into its call status-callback field, exactly as shown — the signature covers the URL", "Select the call events to report: initiated, ringing, answered, completed", "Copy the Auth Token from the console into this connection's signing secret field"]
