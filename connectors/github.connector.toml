# Generated by flux-connectors 0.8.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.8.0"
connector = "github"
vendor = "GitHub"
description = "GitHub REST API: read repositories, issues and pull requests, and write issues and issue comments"
runtime = "http"
base_url = "https://api.github.com"
module = "github.flux"
operations = ["github-repo-get", "github-issue-get", "github-pull-get", "github-issue-create", "github-issue-comment-add"]
