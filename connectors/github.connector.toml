# Generated by flux-connectors 0.17.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.17.0"
connector = "github"
vendor = "GitHub"
description = "GitHub REST API: read repositories, commits, issues, pull requests and Actions workflow runs, and write issues and issue comments"
runtime = "http"
base_url = "https://api.github.com"
module = "github.flux"
operations = ["github-repo-get", "github-issue-get", "github-pull-get", "github-issue-create", "github-issue-comment-add", "github-issue-list", "github-pull-files-list", "github-workflow-run-list", "github-commit-list"]

[[auth]]
name = "github.token"
scheme = "bearer"
env = ["GITHUB_TOKEN"]
description = "GitHub token sent as a bearer — a GitHub App installation token, a fine-grained PAT, or a classic PAT"

[[default_auth]]
credentials = ["github.token"]
