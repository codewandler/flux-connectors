# Generated by flux-connectors 0.17.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.17.0"
connector = "openai"
gid = "com.openai.api:v1"
vendor = "OpenAI"
description = "OpenAI platform: inspect models, stored responses, files and batches, and create chat completions and embeddings"
runtime = "http"
base_url = "https://api.openai.com"
api_version = "v1"
module = "openai.flux"
operations = ["openai-models-list", "openai-model-get", "openai-chat-completion", "openai-embeddings-create", "openai-response-get", "openai-response-input-item-list", "openai-file-list", "openai-batch-list"]

[[auth]]
name = "openai.api_key"
scheme = "bearer"
env = ["OPENAI_API_KEY"]
description = "OpenAI API key, sent as a bearer token"

[[default_auth]]
credentials = ["openai.api_key"]
