# Generated by flux-connectors 0.23.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.23.0"
connector = "microsoft_graph"
service = "files"
gid = "com.microsoft.graph/files:v1.0"
vendor = "Microsoft Graph"
description = "Microsoft Graph: read and reply to Outlook mail, inspect Outlook settings, read and create Calendar events, and read and rename OneDrive files"
runtime = "http"
base_url = "https://graph.microsoft.com"
api_version = "v1.0"
module = "microsoft_graph-files.flux"
operations = ["microsoft_graph-files-item-get", "microsoft_graph-files-item-update"]

[operation_directions]
microsoft_graph-files-item-get = "read"
microsoft_graph-files-item-update = "write"

[operation_semantic_effects]
microsoft_graph-files-item-get = []
microsoft_graph-files-item-update = []

[[auth]]
name = "microsoft_graph.access_token"
scheme = "bearer"
env = ["MICROSOFT_GRAPH_ACCESS_TOKEN"]
description = "Microsoft Entra ID OAuth2 access token sent as a bearer; short-lived, minted outside flux (delegated sign-in or application client-credentials), and refreshed by the host rather than by generated Flux"

[[default_auth]]
credentials = ["microsoft_graph.access_token"]
