# Generated by flux-connectors 0.26.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.26.0"
connector = "resend"
vendor = "Resend"
description = "Resend transactional email: send a message, read what happened to it, and list or inspect the domains a key may send from"
runtime = "http"
base_url = "https://api.resend.com"
module = "resend.flux"
operations = ["resend-email-send", "resend-email-get", "resend-domain-list", "resend-domain-get"]
verify = "resend-domain-list"

[operation_directions]
resend-domain-get = "read"
resend-domain-list = "read"
resend-email-get = "read"
resend-email-send = "write"

[operation_semantic_effects]
resend-domain-get = []
resend-domain-list = []
resend-email-get = []
resend-email-send = []

[[auth]]
name = "resend.api_key"
scheme = "bearer"
env = ["RESEND_API_KEY"]
description = "Resend API key, sent as a bearer token on every request. Minted in the Resend dashboard and scoped there to full access or sending only; this repository holds the reference, never the value"

[[default_auth]]
credentials = ["resend.api_key"]
