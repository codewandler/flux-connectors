# Generated by flux-connectors 0.23.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.23.0"
connector = "front"
vendor = "Front"
description = "Front shared inbox: read and reply to conversations, and tag them"
runtime = "http"
base_url = "https://api2.frontapp.com"
module = "front.flux"
operations = ["front-verify", "front-conversation-list", "front-conversation-get", "front-conversation-message-list", "front-conversation-reply", "front-conversation-tag-add"]
verify = "front-verify"

[operation_directions]
front-conversation-get = "read"
front-conversation-list = "read"
front-conversation-message-list = "read"
front-conversation-reply = "write"
front-conversation-tag-add = "write"
front-verify = "read"

[operation_semantic_effects]
front-conversation-get = []
front-conversation-list = []
front-conversation-message-list = []
front-conversation-reply = []
front-conversation-tag-add = []
front-verify = []

[[auth]]
name = "front.api_token"
scheme = "bearer"
env = ["FRONT_API_TOKEN"]
description = "Front API token sent as a bearer"

[[default_auth]]
credentials = ["front.api_token"]

[[config]]
name = "api_token"
label = "API token"
help = "In Front, open Settings → API & Integrations → API, and create a new token. A token is scoped to the teammate who creates it and to the scopes granted when it is created — replying and tagging require write scopes, not just read ones"
format = "token"
secret = true
docs_url = "https://dev.frontapp.com/docs/authentication"
binds = "credential.front.api_token"
level = "connection"
