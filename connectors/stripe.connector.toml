# Generated by flux-connectors 0.4.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.4.0"
connector = "stripe"
vendor = "Stripe"
description = "Stripe payments: read a balance, customer, charge, payment intent or refund; capture, cancel and refund a payment"
base_url = "https://api.stripe.com"
module = "stripe.flux"
operations = ["stripe-balance-get", "stripe-customer-get", "stripe-charge-get", "stripe-payment-intent-get", "stripe-refund-get", "stripe-payment-intent-capture", "stripe-payment-intent-cancel", "stripe-charge-refund-create"]
