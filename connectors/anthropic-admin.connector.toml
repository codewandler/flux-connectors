# Generated by flux-connectors 0.5.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.5.0"
connector = "anthropic"
service = "admin"
vendor = "Anthropic"
description = "Anthropic platform: the live model catalogue, and organization info, workspaces and API keys from the Admin API"
base_url = "https://api.anthropic.com"
module = "anthropic-admin.flux"
operations = ["anthropic-organization-get", "anthropic-workspaces-list", "anthropic-api-keys-list"]
