# Generated by flux-connectors 0.21.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.21.0"
connector = "freshdesk"
gid = "com.freshdesk.api:v2"
vendor = "Freshdesk"
description = "Freshdesk ticketing: list, read, create and update tickets and the contacts they belong to"
runtime = "http"
base_url = "https://{domain}/api/v2"
api_version = "v2"
module = "freshdesk.flux"
operations = ["freshdesk-test", "freshdesk-ticket-list", "freshdesk-ticket-get", "freshdesk-ticket-create", "freshdesk-ticket-update", "freshdesk-ticket-note-add", "freshdesk-contact-list", "freshdesk-contact-get", "freshdesk-contact-create"]
verify = "freshdesk-test"

[operation_directions]
freshdesk-contact-create = "write"
freshdesk-contact-get = "read"
freshdesk-contact-list = "read"
freshdesk-test = "read"
freshdesk-ticket-create = "write"
freshdesk-ticket-get = "read"
freshdesk-ticket-list = "read"
freshdesk-ticket-note-add = "write"
freshdesk-ticket-update = "write"

[operation_semantic_effects]
freshdesk-contact-create = []
freshdesk-contact-get = []
freshdesk-contact-list = []
freshdesk-test = []
freshdesk-ticket-create = []
freshdesk-ticket-get = []
freshdesk-ticket-list = []
freshdesk-ticket-note-add = []
freshdesk-ticket-update = []

[[config]]
name = "domain"
label = "Freshdesk domain"
help = "Your full Freshdesk host, as it appears in the address bar — `acme.freshdesk.com`, including the `.freshdesk.com` part"
example = "acme.freshdesk.com"
format = "hostname"
docs_url = "https://support.freshdesk.com/en/support/solutions/articles/50000004820"
binds = "endpoint.domain"
level = "connection"
