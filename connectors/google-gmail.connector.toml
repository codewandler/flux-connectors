# Generated by flux-connectors 0.25.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.25.0"
connector = "google"
service = "gmail"
gid = "com.google.api/gmail:v1"
vendor = "Google Workspace"
description = "Google Workspace: read and send Gmail messages, read and create Calendar events, read and rename Drive files"
runtime = "http"
base_url = "https://gmail.googleapis.com"
api_version = "v1"
module = "google-gmail.flux"
operations = ["google-gmail-message-get", "google-gmail-message-send", "google-gmail-labels-list"]

[operation_directions]
google-gmail-labels-list = "read"
google-gmail-message-get = "read"
google-gmail-message-send = "write"

[operation_semantic_effects]
google-gmail-labels-list = []
google-gmail-message-get = []
google-gmail-message-send = []

[[auth]]
name = "google.access_token"
scheme = "bearer"
env = ["GOOGLE_ACCESS_TOKEN"]
description = "Google OAuth2 access token sent as a bearer; short-lived, and refreshed by the host rather than by generated Flux"

[[default_auth]]
credentials = ["google.access_token"]
