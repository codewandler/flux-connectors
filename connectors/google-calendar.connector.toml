# Generated by flux-connectors 0.20.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.20.0"
connector = "google"
service = "calendar"
gid = "com.google.api/calendar:v3"
vendor = "Google Workspace"
description = "Google Workspace: read and send Gmail messages, read and create Calendar events, read and rename Drive files"
runtime = "http"
base_url = "https://www.googleapis.com"
api_version = "v3"
module = "google-calendar.flux"
operations = ["google-calendar-event-get", "google-calendar-event-insert", "google-calendar-calendar-get"]

[operation_semantic_effects]
google-calendar-calendar-get = []
google-calendar-event-get = []
google-calendar-event-insert = []

[[auth]]
name = "google.access_token"
scheme = "bearer"
env = ["GOOGLE_ACCESS_TOKEN"]
description = "Google OAuth2 access token sent as a bearer; short-lived, and refreshed by the host rather than by generated Flux"

[[default_auth]]
credentials = ["google.access_token"]
