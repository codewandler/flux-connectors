# Generated by flux-connectors 0.21.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.21.0"
connector = "sentry"
gid = "io.sentry.api:0"
vendor = "Sentry"
description = "Sentry error tracking: read an issue and its latest event, triage an issue, read a project"
runtime = "http"
base_url = "https://sentry.io"
api_version = "0"
module = "sentry.flux"
operations = ["sentry-issue-get", "sentry-issue-update", "sentry-project-get", "sentry-issue-event-latest"]

[operation_directions]
sentry-issue-event-latest = "read"
sentry-issue-get = "read"
sentry-issue-update = "write"
sentry-project-get = "read"

[operation_semantic_effects]
sentry-issue-event-latest = []
sentry-issue-get = []
sentry-issue-update = []
sentry-project-get = []

[[auth]]
name = "sentry.auth_token"
scheme = "bearer"
env = ["SENTRY_AUTH_TOKEN"]
description = "Sentry auth token sent as a bearer — an organization, user or internal-integration token"

[[default_auth]]
credentials = ["sentry.auth_token"]
