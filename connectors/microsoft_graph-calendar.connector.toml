# Generated by flux-connectors 0.19.1 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.19.1"
connector = "microsoft_graph"
service = "calendar"
gid = "com.microsoft.graph/calendar:v1.0"
vendor = "Microsoft Graph"
description = "Microsoft Graph: read and reply to Outlook mail, inspect Outlook settings, read and create Calendar events, and read and rename OneDrive files"
runtime = "http"
base_url = "https://graph.microsoft.com"
api_version = "v1.0"
module = "microsoft_graph-calendar.flux"
operations = ["microsoft_graph-calendar-event-get", "microsoft_graph-calendar-event-create", "microsoft_graph-calendar-calendar-get", "microsoft_graph-calendar-category-list", "microsoft_graph-calendar-time-zone-list", "microsoft_graph-calendar-language-list"]
verify = "microsoft_graph-calendar-calendar-get"

[operation_semantic_effects]
microsoft_graph-calendar-calendar-get = []
microsoft_graph-calendar-category-list = []
microsoft_graph-calendar-event-create = []
microsoft_graph-calendar-event-get = []
microsoft_graph-calendar-language-list = []
microsoft_graph-calendar-time-zone-list = []

[[auth]]
name = "microsoft_graph.access_token"
scheme = "bearer"
env = ["MICROSOFT_GRAPH_ACCESS_TOKEN"]
description = "Microsoft Entra ID OAuth2 access token sent as a bearer; short-lived, minted outside flux (delegated sign-in or application client-credentials), and refreshed by the host rather than by generated Flux"

[[default_auth]]
credentials = ["microsoft_graph.access_token"]
