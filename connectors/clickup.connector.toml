# Generated by flux-connectors 0.21.0 — do not edit.
# Auth is assembled by the host resolver (`connector-pack`); this manifest becomes a projection of the catalog artifact (C-534).
generator = "flux-connectors 0.21.0"
connector = "clickup"
gid = "com.clickup.api:v2"
vendor = "ClickUp"
description = "ClickUp: list workspaces and a space's folders, and read, list, create and update tasks in a list"
runtime = "http"
base_url = "https://api.clickup.com/api/v2"
api_version = "v2"
module = "clickup.flux"
operations = ["clickup-team-list", "clickup-space-folder-list", "clickup-list-task-list", "clickup-task-get", "clickup-task-create", "clickup-task-update"]
verify = "clickup-team-list"

[operation_directions]
clickup-list-task-list = "read"
clickup-space-folder-list = "read"
clickup-task-create = "write"
clickup-task-get = "read"
clickup-task-update = "write"
clickup-team-list = "read"

[operation_semantic_effects]
clickup-list-task-list = []
clickup-space-folder-list = []
clickup-task-create = []
clickup-task-get = []
clickup-task-update = []
clickup-team-list = []

[[auth]]
name = "clickup.token"
env = ["CLICKUP_TOKEN"]
description = "ClickUp personal API token, sent as the raw value of the Authorization header with no scheme word. Grant access to the workspaces, spaces and lists the operations below need to read or write"

[auth.scheme.header]
name = "Authorization"

[[default_auth]]
credentials = ["clickup.token"]

[[config]]
name = "token"
label = "ClickUp personal API token"
help = "Copy it from ClickUp under your avatar -> Settings -> Apps. ClickUp shows the full token there each time; treat it like a password — it is sent raw, with no scheme word, on every request"
format = "token"
secret = true
docs_url = "https://developer.clickup.com/docs/authentication"
binds = "credential.clickup.token"
level = "connection"
