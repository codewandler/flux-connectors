# Generated by flux-connectors 0.11.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.11.0"
connector = "newrelic"
gid = "com.newrelic.api:2"
vendor = "New Relic"
description = "New Relic APM and alerting over the REST v2 API: list the applications an account is monitoring and read one in detail, read the account's alert policies and its current violations, and list or record deployment markers against an application. The account's region host is configured once, not chosen per call"
runtime = "http"
base_url = "https://{host}/v2"
api_version = "2"
module = "newrelic.flux"
operations = ["newrelic-application-list", "newrelic-application-get", "newrelic-alert-policy-list", "newrelic-alert-violation-list", "newrelic-deployment-list", "newrelic-deployment-create"]

[[config_choices]]
field = "host"
label = "New Relic API host"
kind = "endpoint"
name = "host"

[[config_choices.choices]]
value = "api.newrelic.com"
label = "United States"

[[config_choices.choices]]
value = "api.eu.newrelic.com"
label = "European Union"
