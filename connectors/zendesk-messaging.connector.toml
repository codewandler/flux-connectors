# Generated by flux-connectors 0.15.0 — do not edit.
# Auth and the `http_hosts` allowlist land in C-10.
generator = "flux-connectors 0.15.0"
connector = "zendesk"
service = "messaging"
gid = "com.zendesk.api/messaging:v2"
vendor = "Zendesk"
description = "Zendesk Support ticketing: search, read and update tickets and comments; inspect audits, incremental exports and custom-object definitions"
runtime = "http"
base_url = "https://{subdomain}.zendesk.com/sc"
api_version = "v2"
module = "zendesk-messaging.flux"
operations = ["zendesk-messaging-conversation-create", "zendesk-messaging-conversation-get", "zendesk-messaging-conversation-update", "zendesk-messaging-participant-list", "zendesk-messaging-message-create", "zendesk-messaging-message-list", "zendesk-messaging-user-create", "zendesk-messaging-user-get", "zendesk-messaging-user-update"]
